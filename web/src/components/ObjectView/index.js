import { LayoutType } from '../../infra/specview/SpecViewLayout';

export default function ObjectView ( props ) {

   /* console.log( 'aqio');

    const { layout, data } = props; 

    let templateColumns = '';

    switch ( layout.layoutType ) {

        case LayoutType.SINGLE    : templateColumns = '1fr'
        break;

        case LayoutType.DUPLE     : templateColumns = '1fr 1fr'
        break;

        case LayoutType.TRIPLE    : templateColumns = '1fr 1fr 1fr'
        break;

        case LayoutType.QUADRUPLE : templateColumns = '1fr 1fr 1fr 1fr'
        break;
        
        default : templateColumns = '1fr'
        break;

    }

    const mountLayout = ()  => {

        if ( layout.SpecDTV ) {

            design( layout, data );

        } 

        
    }

    const design = ( layout, data, events ) => {

        const proplist = layout.specDTV.svOProps;  
        
        for ( let i = 0; i <= proplist.length -1; i++ ) {

            console.log( proplist[ i ] );

        }

    }

    mountLayout();*/


}



